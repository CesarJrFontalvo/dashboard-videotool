import { createTheme } from "@mui/material";


export const themeMontserrat = createTheme({
    typography: {
      fontFamily: [
        "Montserrat",
        'sans-serif'
      ].join(','),
    }
  })